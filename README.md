# 🛒 Price Monitor Demo  
_Проект по веб-скрейпингу и анализу цен_

Проект собирает данные о товарах с двух **открытых демо-сайтов**, специально созданных для обучения веб-скрейпингу:

- 📚 **Books to Scrape** – каталог книг: https://books.toscrape.com/  
- 💻 **WebScraper Demo (Laptops)** – демо-магазин ноутбуков:  
  https://webscraper.io/test-sites/e-commerce/allinone/computers/laptops  

Скрипт автоматически:

1. Скачивает страницы с товарами
2. Извлекает название, цену, валюту, ссылку и источник
3. Сохраняет всё в CSV-файл
4. Строит простую аналитику: количество записей, статистика по источникам, топ самых дешёвых товаров

Это отличный пример проекта для портфолио: показывает умение работать с HTTP, HTML, `BeautifulSoup`, `pandas`, организацией пайплайна и структурой кода.

---

## 🚀 Возможности

- Сбор данных с **нескольких сайтов**
- Обход **пагинации** (Books to Scrape)
- Удобная модель данных `Product` (через `@dataclass`)
- Сохранение результатов в `CSV` с отметкой времени
- Простая аналитика:
  - количество записей
  - статистика по магазинам (min/max/mean)
  - топ N самых дешёвых товаров
- Готовый скрипт для запуска из командной строки

---

## 🛠️ Стек технологий

- **Python 3.x**
- `requests` — HTTP-запросы
- `BeautifulSoup` (`beautifulsoup4`) — парсинг HTML
- `pandas` — работа с табличными данными и аналитика

---

## 📁 Структура проекта

```bash
price_monitor_demo/
├─ main.py          # точка входа: оркестрация пайплайна
├─ fetcher.py       # загрузка HTML-страниц (requests + headers)
├─ parsers.py       # парсеры под каждый сайт + dataclass Product
├─ storage.py       # сохранение результатов в CSV
├─ analysis.py      # простая аналитика по собранным данным
├─ requirements.txt # список зависимостей
└─ .gitignore       # игнорируем окружение, кеши и сырые данные
